# Payloads

This directory stores local wordlists and payload references used by the SRC workflow.

- Main imported collection: [`src-payload/README.md`](src-payload/README.md)
- Use these files only inside the active target scope.
- For `ffuf-safe`, prefer small, purpose-matched wordlists over broad brute-force lists.
- High-risk samples under upload/injection categories are not routine fuzz inputs.
